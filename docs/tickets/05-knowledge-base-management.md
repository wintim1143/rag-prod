# 05 — 知识库管理 API

**What to build:** 对持久化索引做文档级管理：列表（含块数与元数据）、删除一篇文档（其全部块从 LanceDB 移除）、按 docId 重索引/增量更新，以及把租户/元数据过滤下推到检索层（demo 权限隔离的正式形态）。全部经 HTTP 端点暴露；检索接口强制带上过滤条件，调用方无法绕过租户范围。

**Blocked by:** 02 — 摄入管线

**Status:** done ✅ (2026-08-14)

- [x] 列表端点返回全部文档 + 块数 + 元数据
- [x] 删除某 docId 后，其所有块从库中消失，后续检索不再返回
- [x] 重索引就地更新 docId（旧块被新块替换）
- [x] 检索支持元数据过滤（如 tenant/docId）；API 层不可达未过滤的跨租户检索
- [x] 生命周期与过滤强制均有测试
