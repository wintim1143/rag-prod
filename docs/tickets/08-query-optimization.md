# 08 — 查询优化（多查询 / HyDE / 改写）

**What to build:** 检索前优化，各策略可配置开关：LLM 查询改写（为检索压缩/扩写 query）、多查询（把问题分解为多个子查询分别检索再 RRF 融合）、可选 HyDE（生成一篇假想文档，向量化后检索）。用 06 的回归运行器对比「优化开 vs 关」的召回/ MRR，证明至少一种策略带来召回提升；同时在对比表中暴露其成本（额外 LLM 调用次数）与延迟影响。

**Blocked by:** 03 — 混合检索 + 本地 cross-encoder 重排；06 — 评估体系

**Status:** done ✅ (2026-08-18)

- [x] 改写 / 多查询 / HyDE 三种策略均可配置开关，且在演示 query 上功能生效
- [x] 回归对比显示至少一种策略开启后召回或 MRR 有提升
- [x] 对比表体现成本/延迟影响（额外 LLM 调用数）
- [x] 各策略均有测试
